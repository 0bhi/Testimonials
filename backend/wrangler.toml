name = "testimonials-backend"
main = "src/index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[dev]
port = 8787

[vars]
# Local development only. Move real secrets to Cloudflare Worker Variables / Secrets for real environments.
# Set these to your Neon HTTP connection string and JWT secret for `wrangler dev` only.
# Example:
# DATABASE_URL = "https://your-neon-endpoint.neon.tech/neondb"
# JWT_SECRET = "dev-secret"
DATABASE_URL = "postgresql://neondb_owner:npg_ajGzV1wmdX7Y@ep-sweet-tree-a1so7or2-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
JWT_SECRET = "some-dev-secret"

[env.production]
name = "testimonials-backend-prod"
[env.production.vars]
# In production, set these via `wrangler secret put` or dashboard; keep them empty here or omit.
DATABASE_URL = ""
JWT_SECRET = ""

[env.staging]
name = "testimonials-backend-staging"
[env.staging.vars]
# Staging Neon HTTP connection string and JWT secret.
DATABASE_URL = ""
JWT_SECRET = ""
